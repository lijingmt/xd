#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){ name=object_name(this_object()); name_cn="【医】甘霖"; unit="本"; picture=name; desc="治疗并净化同房间存活队友\n"; set_item_canDrop(1); set_item_canGet(1); set_item_canTrade(1); set_item_canSend(1); set_item_canStorage(1); value=680; skill_bname="ganlin"; level_limit=60; profe_read_limit="灵医"; need_yushi=230; need_money=0; }
int read(){ int result=::read(); if(read_flag==0) remove(); return result; }
