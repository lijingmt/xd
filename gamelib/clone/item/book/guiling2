#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】龟灵二级";
	unit="本";
	picture=name;
	desc="召唤灵龟附体，在持续时间内提升生命上限（进阶）\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=50;
	skill_bname="guiling";
	skill_level=2;
	level_limit=30;
	profe_read_limit="方士";
	need_yushi=15;
	need_money=400;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
