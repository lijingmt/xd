#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】鹤灵一级";
	unit="本";
	picture=name;
	desc="召唤仙鹤灵兽附体，持续恢复生命并提升防御力\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=50;
	skill_bname="heling";
	level_limit=15;
	profe_read_limit="方士";
	beidong_level=0;
	need_yushi=10;
	need_money=150;
}
int read(){
	int result=::zhudong_read();
	if(read_flag==0){
		remove();
	}
	return result;
}
