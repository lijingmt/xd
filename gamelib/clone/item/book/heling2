#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】鹤灵二级";
	unit="本";
	picture=name;
	desc="召唤仙鹤灵兽附体，持续恢复生命并提升防御力（进阶）\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=50;
	skill_bname="heling";
	skill_level=2;
	level_limit=25;
	profe_read_limit="方士";
	need_yushi=15;
	need_money=400;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
