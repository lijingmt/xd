#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵百雷11级";
	unit="本";
	picture=name;
	desc="召唤大量灵雷攻击当前敌人（强化版）\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=500;
	skill_bname="lingbailei11";
	level_limit=60;
	profe_read_limit="方士";
	need_yushi=390;
	need_money=0;
}
int read(){
	int result=::spec_read("lingbailei");
	if(read_flag==0){
		remove();
	}
	return result;
}
