#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵穿心(2级)";
	unit="本";
	picture=name;
	desc="凝聚灵气，对当前敌人发动强力物理攻击（强化版）\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=650;
	skill_bname="lingchuanxin2";
	level_limit=65;
	profe_read_limit="方士";
	need_yushi=490;
	need_money=0;
}
int read(){
	int result=::spec_read("lingchuanxin");
	if(read_flag==0){
		remove();
	}
	return result;
}
