#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵狐穿";
	unit="本";
	picture=name;
	desc="灵狐附体，在持续时间内提升力量、敏捷和智力\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=140;
	skill_bname="linghuchuan";
	level_limit=14;
	profe_read_limit="方士";
	need_yushi=13;
	need_money=140;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
