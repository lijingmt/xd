#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵虎盾";
	unit="本";
	picture=name;
	desc="灵虎附体，获得攻击和防御双重强化\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=310;
	skill_bname="linghudun";
	level_limit=26;
	profe_read_limit="方士";
	need_yushi=30;
	need_money=310;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
