#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵魂打击";
	unit="本";
	picture=name;
	desc="直接攻击敌人的灵魂，造成高额物理伤害\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=60;
	skill_bname="linghundaji";
	level_limit=4;
	profe_read_limit="方士";
	need_yushi=5;
	need_money=60;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
