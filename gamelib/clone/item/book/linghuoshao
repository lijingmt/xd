#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵火烧";
	unit="本";
	picture=name;
	desc="召唤灵火烧灼敌人，造成持续伤害并降低防御\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=170;
	skill_bname="linghuoshao";
	level_limit=16;
	profe_read_limit="方士";
	need_yushi=16;
	need_money=170;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
