#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵莲铺";
	unit="本";
	picture=name;
	desc="灵莲绽放，治疗自己和队友\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=290;
	skill_bname="linglianpu";
	level_limit=24;
	profe_read_limit="方士";
	need_yushi=28;
	need_money=290;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
