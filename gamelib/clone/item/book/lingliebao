#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵裂爆";
	unit="本";
	picture=name;
	desc="聚能后释放强烈灵爆，造成巨额伤害\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=270;
	skill_bname="lingliebao";
	level_limit=23;
	profe_read_limit="方士";
	need_yushi=26;
	need_money=270;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
