#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵裂兽";
	unit="本";
	picture=name;
	desc="召唤灵裂兽攻击敌人，造成巨额伤害\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=800;
	skill_bname="linglieshou";
	level_limit=70;
	profe_read_limit="方士";
	need_yushi=590;
	need_money=0;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
