#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵群";
	unit="本";
	picture=name;
	desc="召唤灵群攻击，对多个敌人造成伤害并减速\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=250;
	skill_bname="lingqun";
	level_limit=22;
	profe_read_limit="方士";
	need_yushi=24;
	need_money=250;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
