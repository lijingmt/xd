#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵术";
	unit="本";
	picture=name;
	desc="方士的修炼法门，提升灵兽附体的效果\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=50;
	skill_bname="lingshu";
	level_limit=5;
	profe_read_limit="方士";
	beidong_level=1;
	need_yushi=5;
	need_money=50;
}
int read(){
	int result=::beidong_read();
	if(read_flag==0){
		remove();
	}
	return result;
}
