#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵吸术";
	unit="本";
	picture=name;
	desc="以灵力攻击当前敌人，造成物理伤害\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=210;
	skill_bname="lingxishu";
	level_limit=19;
	profe_read_limit="方士";
	need_yushi=20;
	need_money=210;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
