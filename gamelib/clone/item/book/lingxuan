#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵旋";
	unit="本";
	picture=name;
	desc="灵气旋转，攻击当前敌人\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=230;
	skill_bname="lingxuan";
	level_limit=21;
	profe_read_limit="方士";
	need_yushi=22;
	need_money=230;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
