#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵玄影";
	unit="本";
	picture=name;
	desc="化身为灵玄影，在持续时间内提升力量、敏捷和智力\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=350;
	skill_bname="lingxuanying";
	level_limit=28;
	profe_read_limit="方士";
	need_yushi=34;
	need_money=350;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
