#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵玄影(2级)";
	unit="本";
	picture=name;
	desc="化身为灵玄影，大幅提升力量、敏捷和智力（强化版）\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=600;
	skill_bname="lingxuanying2";
	level_limit=65;
	profe_read_limit="方士";
	need_yushi=490;
	need_money=0;
}
int read(){
	int result=::spec_read("lingxuanying");
	if(read_flag==0){
		remove();
	}
	return result;
}
