#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵一触";
	unit="本";
	picture=name;
	desc="瞬发技能，快速攻击敌人并造成流血\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=80;
	skill_bname="lingyichu";
	level_limit=7;
	profe_read_limit="方士";
	need_yushi=7;
	need_money=80;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
