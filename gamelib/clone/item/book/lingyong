#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵涌";
	unit="本";
	picture=name;
	desc="灵力涌动，提升攻击力和法力回复\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=150;
	skill_bname="lingyong";
	level_limit=13;
	profe_read_limit="方士";
	need_yushi=14;
	need_money=150;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
