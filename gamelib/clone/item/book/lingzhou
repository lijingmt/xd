#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】灵咒";
	unit="本";
	picture=name;
	desc="诅咒敌人，使其受到的治疗效果降低\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=90;
	skill_bname="lingzhou";
	level_limit=6;
	profe_read_limit="方士";
	need_yushi=8;
	need_money=90;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
