#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){ name=object_name(this_object()); name_cn="【医】木息"; unit="本"; picture=name; desc="催动草木灵息侵袭目标\n"; set_item_canDrop(1); set_item_canGet(1); set_item_canTrade(1); set_item_canSend(1); set_item_canStorage(1); value=120; skill_bname="muxi"; level_limit=12; profe_read_limit="灵医"; need_yushi=10; need_money=180; }
int read(){ int result=::read(); if(read_flag==0) remove(); return result; }
