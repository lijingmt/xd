#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】三灵合一一级";
	unit="本";
	picture=name;
	desc="三大灵兽同时附体，获得全面强化，是方士的最强绝技\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=50;
	skill_bname="sanlingheyi";
	level_limit=50;
	profe_read_limit="方士";
	beidong_level=0;
	need_yushi=20;
	need_money=1000;
}
int read(){
	int result=::zhudong_read();
	if(read_flag==0){
		remove();
	}
	return result;
}
