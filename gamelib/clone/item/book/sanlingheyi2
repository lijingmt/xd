#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】三灵合一(2级)";
	unit="本";
	picture=name;
	desc="三大灵兽同时附体，获得全面强化（强化版）\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=700;
	skill_bname="sanlingheyi2";
	level_limit=65;
	profe_read_limit="方士";
	need_yushi=490;
	need_money=0;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
