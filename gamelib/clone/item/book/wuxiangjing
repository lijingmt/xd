#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){ name=object_name(this_object()); name_cn="【无】无相净"; unit="本"; picture=name; desc="无相净化，按优先级解一项负面\n"; set_item_canDrop(1); set_item_canGet(1); set_item_canTrade(1); set_item_canSend(1); set_item_canStorage(1); value=600; skill_bname="wuxiangjing"; level_limit=36; profe_read_limit="无相"; need_yushi=18; need_money=600; }
int read(){ int result=::read(); if(read_flag==0) remove(); return result; }
