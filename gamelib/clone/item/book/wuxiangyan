#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){ name=object_name(this_object()); name_cn="【无】无相焰"; unit="本"; picture=name; desc="无相横向群攻，6秒轮转、发挥法术专精60%威力；随可用阶段覆盖2至10个合法目标\n"; set_item_canDrop(1); set_item_canGet(1); set_item_canTrade(1); set_item_canSend(1); set_item_canStorage(1); value=380; skill_bname="wuxiangyan"; level_limit=31; profe_read_limit="无相"; need_yushi=2; need_money=380; }
int read(){ int result=::read(); if(read_flag==0) remove(); return result; }
