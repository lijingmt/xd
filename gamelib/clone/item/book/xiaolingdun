#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){
	name=object_name(this_object());
	name_cn="【方】小灵盾";
	unit="本";
	picture=name;
	desc="凝聚灵气形成小护盾，抵挡少量伤害\n";
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	value=40;
	skill_bname="xiaolingdun";
	level_limit=2;
	profe_read_limit="方士";
	need_yushi=3;
	need_money=40;
}
int read(){
	int result=::read();
	if(read_flag==0){
		remove();
	}
	return result;
}
