#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){ name=object_name(this_object()); name_cn="【象】星壁"; unit="本"; picture=name; desc="展开可耗尽个人星光护盾的法门\n"; set_item_canDrop(1); set_item_canGet(1); set_item_canTrade(1); set_item_canSend(1); set_item_canStorage(1); value=150; skill_bname="xingbi"; level_limit=15; profe_read_limit="天象"; need_yushi=12; need_money=250; }
int read(){ int result=::read(); if(read_flag==0) remove(); return result; }
