#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){ name=object_name(this_object()); name_cn="【象】星落"; unit="本"; picture=name; desc="消耗星痕换取受控爆发的核心法门\n"; set_item_canDrop(1); set_item_canGet(1); set_item_canTrade(1); set_item_canSend(1); set_item_canStorage(1); value=500; skill_bname="xingluo"; level_limit=60; profe_read_limit="天象"; need_yushi=390; need_money=0; }
int read(){ int result=::read(); if(read_flag==0) remove(); return result; }
