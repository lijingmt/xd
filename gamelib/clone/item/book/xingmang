#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){ name=object_name(this_object()); name_cn="【象】星芒"; unit="本"; picture=name; desc="天象入门星术，命中后凝聚一层星痕\n"; set_item_canDrop(1); set_item_canGet(1); set_item_canTrade(1); set_item_canSend(1); set_item_canStorage(1); value=50; skill_bname="xingmang"; level_limit=1; profe_read_limit="天象"; need_yushi=2; need_money=30; }
int read(){ int result=::read(); if(read_flag==0) remove(); return result; }
