#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_BOOK;
protected void create(){ name=object_name(this_object()); name_cn="【神】周天静止"; unit="本"; picture=name; desc="失落的天象大神传承，只由70级以上强敌极低概率掉落\n"; set_item_canDrop(1); set_item_canGet(1); set_item_canTrade(1); set_item_canSend(1); set_item_canStorage(1); value=5000; skill_bname="zhoutianjingzhi"; level_limit=80; profe_read_limit="天象"; need_yushi=0; need_money=0; }
int read(){ int result=::read(); if(read_flag==0) remove(); return result; }
