#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_COMBINE_ITEM;
protected void create(){
	name=object_name(this_object());
	name_cn="离火玉";
	unit="枚";
	desc="蕴含离火之气的玉髓，提炼装备的必需材料；可用碎玉一比一购买\n";
	value=1000;
	amount=1;
	picture=name;
	set_item_canEquip(0);
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	set_for_material("refine");
	set_add_luck(0);
}
