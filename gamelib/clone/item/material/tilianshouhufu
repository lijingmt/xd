#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_COMBINE_ITEM;
protected void create(){
	name=object_name(this_object());
	name_cn="提炼守护符";
	unit="张";
	desc="蕴含守护之力的古老符箓：提炼门槛级失败时自动消耗，降级惩罚从30%减轻为3级\n";
	value=100000;
	amount=1;
	picture=name;
	set_item_canEquip(0);
	set_item_canDrop(0);
	set_item_canGet(1);
	set_item_canTrade(0);
	set_item_canSend(1);
	set_item_canStorage(1);
	set_for_material("refine");
	set_add_luck(0);
}
