#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_DANYAO;

protected void create()
{
	name = object_name(this_object());
	name_cn = "【追赶】三倍曜光露";
	desc = "打怪总经验提升至3倍，持续30分钟。\n";
	desc += "仅69级及以下可以使用；同类经验药效果不会叠加，新效果会覆盖旧效果。\n";
	desc += "此物品不能丢弃、交易、转赠或存入仓库。\n";
	unit = "瓶";
	picture = name;
	set_item_canDrop(0);
	set_item_canGet(1);
	set_item_canTrade(0);
	set_item_canSend(0);
	set_item_canStorage(0);
	set_danyao_kind("te_exp");
	set_danyao_type("exp");
	set_effect_value(200);
	set_danyao_timedelay(1800);
	set_danyao_max_level(69);
	set_item_level(1);
}
