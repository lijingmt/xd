#include <globals.h>
#include <wapmud2/include/wapmud2.h>
inherit MUD_ITEM;
inherit WAP_F_VIEW_PICTURE;
inherit WAP_F_VIEW_LINKS;
inherit WAP_F_VIEW_VALUE;

/*
//获得物品的价格 added by caijie 08/11/07
int query_item_price(){
	object ob = this_object();
	int price = 0;
	if(ob->is("combine_item")){
		if(ob->query_item_type()=="food"||ob->query_item_type()=="water"){
			price = (ob->level_limit*50/4)*ob->amount;
		}
		else if(ob->query_for_material() != ""){
			price = ob->query_value()*items[i]->amount;
		}
	}
	else{
		price = ob->query_item_canLevel()*50/4;
	}
	return price;
}
*/
